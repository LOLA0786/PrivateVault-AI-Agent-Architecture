rules:
  - condition:
      sensitivity: HIGH
      contains_pii: true
    action: BLOCK

  - condition:
      sensitivity: HIGH
    action: BLOCK

  - condition:
      sensitivity: LOW
    action: ALLOW
